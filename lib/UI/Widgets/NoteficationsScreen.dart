import 'package:flutter/cupertino.dart';
import 'package:flutter/material.dart';

class NoteficationsScreen extends StatelessWidget {
  const NoteficationsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body: Center(
        child: Text('Noteficatiosn Screen '),
      ),
    );
  }
}
